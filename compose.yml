services:
  torznab-bridge:
    image: ghcr.io/immatheushen/torznab-bridge:latest
    build:
      context: ./addon
      dockerfile: Dockerfile.torznab
    container_name: torznab-bridge
    restart: unless-stopped
    ports:
      - "9699:9699"
    volumes:
      - ./data:/config
